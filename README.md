This is a nodejs port from stream-framework by getstream.

Road map
- Support any type of id (now only integer, and it is problematic for string id)
  - sorted set support of integer rank
- port aggregate
